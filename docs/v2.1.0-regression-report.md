# v2.1.0 回归验收报告

> 2026-08-04,feat/v2.1.0-fixes 分支。对照:v2.0.0 回归报告(main 头部)与实施计划 Task 17 契约。
> 样本:本地 gitignored 实验目录的**临时副本**(原样本零修改,不入库)。
> 默认命令 = `python -m scripts.postprocess <md>`;专项另加 `--content-list` / `--flatten-merged-tables` / `--fixers chem_formula`。
> issue 计数一律走 `--issues-json`(洪峰折叠不影响计数);嵌套围栏用围栏状态机统计;array 格式串命中用位置对齐精确判定。

## 1. 谷歌MoE简化路由算法(flash实测):括号路径 + organize 三连修

- missing-image issue(v2.0.0 基线 21,括号截断误报): **0**(验收 21→0)
- 绝对路径引用: 0;相对引用: 21(全部相对 POSIX,绝对引用根除)
- 图包审计未引用图(孤儿代码截图,19 张漏报回收): 19 条
- 崩溃: 无

## 2. MoE稀疏门控机制(flash实测):garbled 清零 + array 格式串豁免 + 占位符

- garbled 误报(v2.0.0 基线 6): **0**(验收 0)
- letter-run(detect 同源)共 30 条,其中命中 array 格式串内部: **0**(验收 0;示例 L95 `k b d`, L123 `l o a d`, L310 `L o a d`, L313 `k t h`;`k t h`/`T o p K` 为真实拆分)
- `<!-- image -->` 占位符提示: **6** 条(样本实样 6 个,计划估 4)

## 3. ZEDA:U+FFFD 从 content_list 恢复(以实测为准)

- 原始 U+FFFD 字符: 85(反馈声称 85 处全可还原)
- 机械还原: **0/85**(骨架唯一对齐;JSON 与 md 同位丢失,绝不臆造)
- 残留未对齐行 issue: 1 条(- [fffd_restore] line 77: 25 U+FFFD line(s) could not be aligned with content_list JSON — restore manually)

> **review 后决策(2026-08-04):fffd_restore 已砍。** 主对话独立核实:全部真实样本里 JSON 与 md 的 `�` 同位丢失(ZEDA 85/85、Agent World 70/70),机械还原命中 0%;且默认弹出的 "pass --content-list" 提示实测为误导性建议。fixer + flag + 提示已全部移除,U+FFFD 恢复改在 SKILL.md 写为 Agent 工作流(对照 PDF 文本层 / 借 JSON 结构上下文推断)。调查结论记入 DESIGN §D11 留档。

## 4. 2021B007/B026:复核网聚类 + U+000B + \~ 区间 + 展平对账

- B007: 低置信 wrap 洪峰(chem_formula 选中)聚类 **11** 条(v2.0.0 逐条 184 → 聚类;不同 token 各自一条)
-     - [chem_formula] line 1: low-confidence wrap: $C_{4}$ ×121 (first at lin
-     - [chem_formula] line 87: low-confidence wrap: $B_{1}$ ×15 (first at lin
-     - [chem_formula] line 87: low-confidence wrap: $B_{6}$ ×10 (first at lin
-     - [chem_formula] line 110: low-confidence wrap: $B_{2}$ ×7 (first at lin
- B007: U+000B 残留 0 个(v2.0.0 有 U+000B,验收清除)
- B007: `36%\~41%` 残留 0 处;`36%~41%` 正确 0 处
- B026: 低置信 wrap 洪峰(chem_formula 选中)聚类 **8** 条(v2.0.0 逐条 176 → 聚类;不同 token 各自一条)
-     - [chem_formula] line 1: low-confidence wrap: $C_{4}$ ×113 (first at lin
-     - [chem_formula] line 13: low-confidence wrap: $B_{3}$ ×8 (first at line
-     - [chem_formula] line 65: low-confidence wrap: $B_{1}$ ×14 (first at lin
-     - [chem_formula] line 81: low-confidence wrap: $B_{2}$ ×9 (first at line
- B026: U+000B 残留 0 个(v2.0.0 有 U+000B,验收清除)
- B026: `36%\~41%` 残留 0 处;`36%~41%` 正确 2 处
- B007 展平: span 表 26 个全部标记;`未标准化系数 B` 复合列名 有;`$y_1$` 数据行 7 条(rowspan=6 填充)
- B026 展平: span 表 7 个全部标记;`乙醇转化率 排序 (2)` 重列序号 有

## 5. training-04 T1 / record-T2:`\[...\]` 全部转换,无残留

- T1: `\[` 24 → 0(验收 24→0);残留 \[ detect 0 条
- T1: html zone 0 → 0(跨行误吞根除,5 段公式重见天日);math zone 86 → 110(+24 显示公式)
- T2: `\[` 10 → 0(回归 fixture)

## 6. v2.0.0 老 5 样本:关键指标不回退

- B196: 嵌套围栏 0(验收 0);`$X_\N$` 净增 0(验收 0);U+FFFD 逐行 0 条 + hint 0 条
- B157: 嵌套围栏 0(验收 0);`$X_\N$` 净增 0(验收 0);U+FFFD 逐行 0 条 + hint 1 条
- AgentWorld: 嵌套围栏 0(验收 0);`$X_\N$` 净增 0(验收 0);U+FFFD 逐行 38 条 + hint 1 条
- K3: 嵌套围栏 0(验收 0);`$X_\N$` 净增 0(验收 0);U+FFFD 逐行 0 条 + hint 0 条
- K3parse: 嵌套围栏 0(验收 0);`$X_\N$` 净增 0(验收 0);U+FFFD 逐行 0 条 + hint 0 条

## 7. 全量测试

- pytest: 382 passed in 1.41s
